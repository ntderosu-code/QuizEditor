enum AppCopy {
    static let checkQuiz = "Check Quiz"
    static let aiSuggestions = "AI Suggestions"
    static let offlineChecks = "Offline checks"
    /// The per-question action. It appears in the editor, the question's
    /// overflow menu, and the AI panel, and used to be named differently in
    /// each of the three.
    static let draftFeedback = "Draft Feedback"
    /// The quiz-level action, named apart because it runs over every question.
    static let draftFeedbackForQuiz = "Draft Student Feedback"
}
